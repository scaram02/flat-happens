import React from "react";

const Invite = props => {
  return <div>Your flat has been successfully created. You can invite your flatmates by sharing this link with them.
  </div>;
};

export default Invite;
